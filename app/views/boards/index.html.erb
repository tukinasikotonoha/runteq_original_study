<div class="container pt-3">
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @boards.empty? %>
          <p><%= t(".not_board") %></p>
        <% else %>
          <%= render @boards %>
        <% end %>
      </div>
    </div>
  </div>
</div>